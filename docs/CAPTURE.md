# Capture / deliverables

Required under `public/outreach/`:

1. `before-original-desktop.png` — live bourkeconstructionoc.com, not a recreation
2. `after-desktop.png` — ~1440px, full page preferred
3. `after-mobile.png` — ~390px
4. `after-scroll.gif` — intentional scroll past the hero
5. `after-scroll.mp4` — same

`/outreach` must preview all five.

Do not capture until desktop + mobile QA is stable.
